namespace DroidsCode.DroidCore
{
    // Apenas os 6 atributos base nesta fase. NAO expandir (DEF, FLEE, ATK%, etc.)
    // sem confirmar com a equipe — ver LEIA_PRIMEIRO.md, pendencia 6.
    public enum TipoAtributo { For, Agi, Vit, Int, Dex, Luk }
}
